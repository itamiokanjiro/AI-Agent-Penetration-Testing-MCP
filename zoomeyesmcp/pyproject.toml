[project]
name = "zoomeye-mcp"
version = "0.1.0"
description = "ZoomEye MCP server (uv runnable)"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31.0",
    "mcp>=1.0.0,<2",
]

[tool.uv]
package = false
